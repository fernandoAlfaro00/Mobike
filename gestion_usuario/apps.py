from django.apps import AppConfig


class GestionUsuarioConfig(AppConfig):
    name = 'gestion_usuario'

    # def ready(self): #method just to import the signals
    # 	import gestion_usuario.signals
